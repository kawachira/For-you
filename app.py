import streamlit as st
import yfinance as yf

st.title("📡 ทดสอบการเชื่อมต่อ Yahoo Finance")

symbol = st.text_input("ชื่อหุ้น", value="AAPL").upper()

if st.button("ทดสอบดึงข้อมูล"):
    st.write(f"กำลังดึงข้อมูล: {symbol} ...")
    
    try:
        # ดึงข้อมูลแบบดิบๆ ไม่ผ่านการตกแต่ง
        stock = yf.Ticker(symbol)
        df = stock.history(period="5d") # ขอแค่ 5 วันพอ
        
        if df.empty:
            st.error("❌ ดึงข้อมูลสำเร็จ แต่ได้ 'ตารางเปล่า' (Empty DataFrame)")
            st.warning("สาเหตุ: Yahoo Finance กำลังบล็อก IP ของ Server นี้อยู่ครับ (ต้องรอสักพัก หรือเปลี่ยนเวลาเล่น)")
        else:
            st.success(f"✅ เชื่อมต่อสำเร็จ! ได้ข้อมูลมา {len(df)} แถว")
            st.dataframe(df) # โชว์ตารางข้อมูลดิบ
            
    except Exception as e:
        st.error(f"❌ Error เกิดข้อผิดพลาด: {e}")
